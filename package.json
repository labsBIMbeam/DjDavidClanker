{
  "name": "dj-david-clanker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "DJ David Clanker - a two-deck auto-DJ napplet (NIP-5D): Wavlake value4value, your own Navidrome crate, Audius/Jamendo/Archive discovery",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite --port 5173 --host 127.0.0.1",
    "build": "vite build",
    "build:standalone": "vite build --mode standalone",
    "preview": "vite preview --mode standalone --port 4173 --host 127.0.0.1",
    "shell": "vite build && vite build --mode standalone && node dev/serve-shell.mjs",
    "conformance": "napplet-conformance dist"
  },
  "dependencies": {
    "@napplet/sdk": "^0.28.0"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@napplet/conformance-cli": "^0.2.19",
    "@napplet/shim": "^0.30.0",
    "@napplet/vite-plugin": "^0.14.1",
    "vite": "^8.2.0"
  }
}
